[package]
name = "context-smith"
version = "0.3.0"
edition = "2021"
description = "AI context compiler — extract task-relevant code from Git repos"
license = "MIT"
repository = "https://github.com/flipslidersand/context-smith"
homepage = "https://github.com/flipslidersand/context-smith"
readme = "README.md"
keywords = ["context", "llm", "bm25", "code-search", "cli"]
categories = ["command-line-utilities", "development-tools"]
exclude = ["errors/", "docs/", ".claude/"]

[lib]
name = "context_smith"
path = "src/lib.rs"

[[bin]]
name = "contextsmith"
path = "src/main.rs"

[dependencies]
git2         = "0.19"
tree-sitter  = "0.24"
tree-sitter-rust   = "0.23"
tree-sitter-python = "0.23"
tree-sitter-go     = "0.23"
tree-sitter-typescript = "0.23"
tree-sitter-javascript = "0.23"
rusqlite     = { version = "0.31" }
petgraph     = "0.6"
serde        = { version = "1", features = ["derive"] }
serde_json   = "1"
clap         = { version = "4", features = ["derive"] }
anyhow       = "1"
walkdir      = "2"
ureq         = { version = "2", features = ["json"], optional = true }
tree-sitter-ruby = "0.23"

[features]
# bundled-sqlite: compile SQLite from source (zero system deps, works everywhere).
# Disable with --no-default-features if you have libsqlite3-dev installed and want
# faster incremental builds (avoids recompiling ~300 kLOC of C on cold builds).
default = ["bundled-sqlite"]
bundled-sqlite = ["rusqlite/bundled"]
# Enable remote embedding-service integration (semantic search + RRF fusion).
# Off by default so the standard build stays lean and works offline (BM25 only).
remote-embed = ["dep:ureq"]

[dev-dependencies]
tempfile = "3"
